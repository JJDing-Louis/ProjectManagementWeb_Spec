# 2026-09-24 規格同步與 UI 擷取日誌

## 範圍

- Backend：Controllers、Application contracts、Infrastructure services、`ApplicationDbContext`、migrations、model snapshot 與既有測試。
- Frontend：Vue Router、Views、HTTP adapters、DTO／ViewModel、Pinia stores、Vitest 與 Playwright。
- Spec：C4、Flowchart、Schema、StaticData、User Story、Implementation Backlog、Test Cases、UI Mock 與索引。

## 程式碼基準

| Repository | Branch／Commit | 工作樹處理 |
|---|---|---|
| Backend | `develop@9721918` | 既有 README／docs 變更只讀取，不修改 |
| Frontend | `develop@8078669` | 既有 README／docs 變更只讀取；暫時 UI 擷取測試執行後已移除 |
| Spec | `main@f598377` | 本輪文件與 PNG 更新目標 |

## 實際 UI 擷取

- 命令路徑：沿用 Frontend `scripts/run-isolated-e2e.sh` 與 `playwright.config.ts`。
- 環境：Desktop Chrome 1440×900、獨立 Compose project、全新 SQL volume、臨時 Bootstrap Admin／Owner、空白 SMTP credentials。
- 資料流程：登入、讀取使用者、建立 Project、建立 Task、開啟各 route，再將畫面保存至 `UIMock/`。
- 結果：Passed 1、Failed 0；16 張 PNG 均為 1440×900。
- 清理：擷取用測試檔已刪除；Compose containers、network 與 volumes 由腳本清除。

## 現行程式碼盤點

- Backend：40 個 `/api/v1` Controller actions。
- Database：model snapshot 共有 26 張 EF Application tables。
- Owner：建立時自動建立 membership 與 `ProjectManager`；移交時確保新 Owner 具有 `ProjectManager`，不自動移除舊 Owner 的 Project Roles。
- Task 顯示：API 只回傳建立者／指派對象 Account ID；前端優先從已載入的 Project members 轉成顯示名稱，找不到時顯示 GUID。
- Localization：繁體中文模式仍有部分直接寫在 Vue 元件內的英文文字，UI Mock 按現行畫面保留。

## 驗證邊界

- 本輪 UI 擷取案例證明現行畫面能在隔離前後端環境完成核心導覽與建立流程，但不是正式產品測試案例的一部分。
- 正式全量證據仍為 2026-09-21：Backend Unit 61、Integration 101、Frontend Vitest 79、Playwright desktop／mobile 26，均為 Failed 0、Skipped 0。
- C4 彙整版的五個 Mermaid block 與五份獨立 `.mmd` 逐一比對，結果為 5／5 完全一致。
- C4、11 份 Flowchart 與 Schema 共 18 個 Mermaid 文件來源均通過安全靜態 lint；此檢查不等於瀏覽器或 `mmdc` 原生 renderer acceptance。
- `git diff --check` 通過；118 個 `TC-*` 案例標題與 16 張 UI PNG 均完整保留。
